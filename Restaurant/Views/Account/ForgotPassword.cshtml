<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Forgot Password</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" />
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,900&display=swap" rel="stylesheet">
    <style>
        section.login-tabs.padding-large {
            background: transparent;
            min-height: 80vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            padding: 0;
        }

        .forgot-bg-img {
            position: absolute;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            object-fit: cover;
            z-index: 0;
            filter: brightness(0.55) blur(3px) grayscale(0.06) saturate(1.15);
            transition: filter 0.7s cubic-bezier(.4,0,.2,1);
            pointer-events: none;
        }

        .forgot-password-panel {
            background: rgba(255,255,255,0.20);
            backdrop-filter: blur(28px) saturate(220%);
            -webkit-backdrop-filter: blur(28px) saturate(220%);
            border-radius: 36px;
            box-shadow: 0 20px 64px 0 rgba(31,38,135,0.22), 0 0 0 2px #fff4;
            padding: 64px 48px 48px 48px;
            position: relative;
            z-index: 2;
            animation: fadeInUp 1.2s cubic-bezier(.39,.575,.56,1.000) both;
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: box-shadow 0.5s cubic-bezier(.4,0,.2,1), background 0.5s cubic-bezier(.4,0,.2,1);
        }

            .forgot-password-panel h2, .forgot-password-panel button.nav-link {
                font-size: 2.5rem;
                font-weight: 900;
                color: #FF4B2B;
                letter-spacing: 3px;
                text-shadow: 0 2px 18px #fff8, 0 0 2px #FF4B2B44;
                text-transform: uppercase;
                margin-bottom: 22px;
                background: none;
                border: none;
                box-shadow: none;
                transition: color 0.3s cubic-bezier(.4,0,.2,1);
            }

            .forgot-password-panel input.forgot-input {
                background: rgba(255,255,255,0.96);
                border-radius: 16px;
                border: 1.5px solid #eee;
                font-size: 1.18rem;
                padding: 18px 22px;
                margin-bottom: 22px;
                box-shadow: 0 2px 16px rgba(31,38,135,0.08);
                transition: box-shadow 0.3s, background 0.3s, border 0.3s;
            }

                .forgot-password-panel input.forgot-input:focus, .forgot-password-panel input.forgot-input:hover {
                    background: #fffbe7;
                    border: 1.5px solid #fcb045;
                    box-shadow: 0 0 0 4px #fcb04555;
                }

            .forgot-password-panel .forgot-btn {
                background: linear-gradient(90deg, #f7971e 0%, #ffd200 50%, #f7971e 100%);
                color: #fff;
                font-weight: 900;
                letter-spacing: 2px;
                border: none;
                border-radius: 22px;
                padding: 18px 0;
                font-size: 1.28rem;
                box-shadow: 0 8px 32px rgba(255,215,0,0.18), 0 2px 16px #fd1d1d22;
                transition: background 0.3s, box-shadow 0.3s, transform 0.2s, color 0.2s;
                position: relative;
                overflow: hidden;
                text-shadow: 0 1px 8px #fff8;
            }

                .forgot-password-panel .forgot-btn::before {
                    content: '';
                    position: absolute;
                    left: -80%;
                    top: 0;
                    width: 60%;
                    height: 100%;
                    background: linear-gradient(120deg, rgba(255,255,255,0.35) 0%, rgba(255,255,255,0.0) 100%);
                    transform: skewX(-20deg);
                    transition: left 0.5s cubic-bezier(.4,0,.2,1);
                    z-index: 1;
                }

                .forgot-password-panel .forgot-btn:hover::before {
                    left: 120%;
                }

                .forgot-password-panel .forgot-btn:hover {
                    background: linear-gradient(90deg, #ffd200 0%, #f7971e 100%);
                    box-shadow: 0 20px 56px #ffd20044, 0 2px 16px #fd1d1d33;
                    color: #222;
                    transform: translateY(-4px) scale(1.07);
                }

       
    </style>
</head>
<body>
    <section id="banner" class="py-3" style="background: #F9F3EC;">
        <div class="container">
            <div class="hero-content py-5 my-3">
                <h2 class="display-1 mt-3 mb-0">Account</h2>
                <nav class="breadcrumb">
                 
                    <span class="breadcrumb-item active" aria-current="page">Account</span>
                </nav>
            </div>
        </div>
    </section>
    <section class="login-tabs padding-large" style="background: transparent; min-height: 80vh; display: flex; align-items: center; justify-content: center; position: relative;">
        <img class="forgot-bg-img" src="https://images.unsplash.com/photo-1519125323398-675f0ddb6308?auto=format&fit=crop&w=1500&q=80" alt="Forgot Password Background">
        <div class="container my-5 py-5">
            <div class="row justify-content-center">
                <div class="col-lg-6">
                    <div class="forgot-password-panel">
                        <button class="nav-link mx-3 fs-3 border-bottom border-dark-subtle border-0 text-uppercase active"
                                id="nav-sign-in-tab" data-bs-toggle="tab" data-bs-target="#nav-sign-in" type="button"
                                role="tab" aria-controls="nav-sign-in" aria-selected="true">
                            Forgot Password
                        </button>
                        <form id="form1" class="form-group flex-wrap" method="post">
                            <div class="form-input my-4">
                                <input name="email" required type="email" placeholder="Enter email address" class="form-control mb-3 p-4 forgot-input">
                                <div asp-validation-summary="ModelOnly"></div>
                                <div class="d-grid my-3">
                                    <button type="submit" class="btn btn-dark btn-lg rounded-1 forgot-btn">Send reset link</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>
</html>
